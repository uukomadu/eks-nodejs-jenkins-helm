replicaCount: 1

image:
  repository: 506570851351.dkr.ecr.us-east-2.amazonaws.com/node-app
  tag: 20794b0c2f319904b4469f77f392e0778705eab0
  pullPolicy: Always

service:
  # Instance-mode ALBs route through a node port instead of directly to pod ENIs.
  type: NodePort
  port: 80
  targetPort: 3000

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

probes:
  path: /
  readiness:
    initialDelaySeconds: 5
    periodSeconds: 10
  liveness:
    initialDelaySeconds: 15
    periodSeconds: 20

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 50
  targetMemoryUtilizationPercentage: 50

# Spread HPA-created replicas across worker nodes whenever multiple nodes exist.
topologySpreadConstraints:
  enabled: true
  maxSkew: 1
  topologyKey: kubernetes.io/hostname
  whenUnsatisfiable: DoNotSchedule

ingress:
  enabled: true
  className: alb
  annotations:
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: instance
    alb.ingress.kubernetes.io/healthcheck-path: /
  path: /
  pathType: Prefix
